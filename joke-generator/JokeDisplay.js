import React from 'react';\nimport { useJokes } from '../JokeContext';\n\nconst JokeDisplay = () => {\n  const { currentJoke, isFavorited, addToFavorites, removeFromFavorites } =\n    useJokes();\n\n  if (!currentJoke) {\n    return (\n      <div className=\"joke-display empty\">\n        <p>Click 'Get Joke' to start laughing! 😄</p>\n      </div>\n    );\n  }\n\n  const jokeType = currentJoke.type;\n  const handleFavorite = () => {\n    if (isFavorited(currentJoke)) {\n      removeFromFavorites(Date.now());\n    } else {\n      addToFavorites(currentJoke);\n    }\n  };\n\n  return (\n    <div className=\"joke-display\">\n      <div className=\"joke-content\">\n        {jokeType === 'twopart' ? (\n          <>\n            <p className=\"joke-setup\">{currentJoke.setup}</p>\n            <p className=\"joke-delivery\">{currentJoke.delivery}</p>\n          </>\n        ) : (\n          <p className=\"joke-single\">{currentJoke.joke}</p>\n        )}\n      </div>\n      <div className=\"joke-meta\">\n        <span className=\"joke-category\">{currentJoke.category}</span>\n        <button\n          className={`favorite-btn ${isFavorited(currentJoke) ? 'favorited' : ''}`}\n          onClick={handleFavorite}\n        >\n          {isFavorited(currentJoke) ? '❤️' : '🤍'}\n        </button>\n      </div>\n    </div>\n  );\n};\n\nexport default JokeDisplay;